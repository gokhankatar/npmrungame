export interface StoreNpmRunGame {
  theme?: "dark" | "light"
}

export interface NavbarList {
  title: string
  path: string
  icon: string
}

export interface Game_Category {
  name: string;
  icon: string;
}